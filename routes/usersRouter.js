// import { Router } from 'express';
// import { login, logout, signup } from '../controllers/auth.controller.js';
// import { deleteOne, getAll, getOne } from '../controllers/crud.js';
// import User from '../models/User.js';
// import authenticate from '../middlewares/verifyToken.js';

// const usersRouter = Router();

// usersRouter.route('/').post(signup).get(getAll(User));
// usersRouter.route('/login').post(login);
// usersRouter.route('/logout').delete(logout);

// usersRouter.route('/:id').get(getOne(User)).delete(authenticate, deleteOne(User));
// // usersRouter.route('/:id').get(getSinglePost).put(validateZod(postSchema), updatePost).delete(deletePost);

// export default usersRouter;
